services:
  - type: web
    name: EVRoute-AI-Web
    env: static
    rootDir: evroute-frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html